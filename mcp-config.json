{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/specification/main/schema/mcp_config_schema.json",
  "description": "MCP configuration for Netlify integration",
  "mcpServers": {
    "netlify": {
      "command": "node",
      "args": ["./mcp-server.js"],
      "env": {
        "NETLIFY_AUTH_TOKEN": "nfp_J3rfoaaCgJgYMYaZodLkJCSMXTDFjtk34080",
        "NODE_ENV": "production"
      },
      "disabled": false,
      "autoApprove": [
        "netlify_status",
        "netlify_sites_list",
        "netlify_site_info",
        "netlify_env_list",
        "netlify_functions_list",
        "netlify_logs"
      ],
      "description": "Netlify MCP Server for managing deployments, sites, and environment variables"
    }
  }
}