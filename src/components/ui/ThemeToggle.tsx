import { useTheme } from '../../contexts/ThemeContext';

// ThemeToggle component is now hidden as we always use the futuristic theme
const ThemeToggle = () => {
  // Return null to hide the component
  return null;
};

export default ThemeToggle;